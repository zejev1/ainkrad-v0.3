name: Ainkrad CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          fetch-depth: 0
          persist-credentials: false

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install dependencies
        run: npm ci --no-audit --no-fund

      - name: Reproduce reported shoreline failure on the published base
        if: github.event_name == 'pull_request' && github.head_ref == 'work/ainkrad-v021-fix2'
        run: |
          git worktree add --detach "$RUNNER_TEMP/ainkrad-base" 68fdaabfc0ea4ae6cae1c6232141d2088dcc3684
          ln -s "$GITHUB_WORKSPACE/node_modules" "$RUNNER_TEMP/ainkrad-base/node_modules"
          npx --no-install tsx scripts/reproduce-shore.ts "$RUNNER_TEMP/ainkrad-base"

      - name: Typecheck
        run: npm run typecheck

      - name: Tests
        run: npm run test -- --reporter=default --reporter=json --outputFile.json="$RUNNER_TEMP/ainkrad-tests.json"

      - name: Production build
        run: npm run build
      - name: Audit and prepare SPCK checkout
        id: package
        if: github.event_name == 'pull_request' && github.head_ref == 'work/ainkrad-v021-fix2'
        run: node scripts/package-spck.mjs
      - name: Upload SPCK ZIP
        if: github.event_name == 'pull_request' && github.head_ref == 'work/ainkrad-v021-fix2'
        uses: actions/upload-artifact@v4
        with:
          name: Ainkrad21Fix3
          path: ${{ steps.package.outputs.directory }}
          include-hidden-files: true
          if-no-files-found: error
          retention-days: 30
